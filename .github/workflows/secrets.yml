name: secrets
on:
  push:
    branches:
      - xxx
  # pull_request:
  #   types: [opened, reopened, synchronize]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo This is my secret ${{secrets.MY_SECRET}}
