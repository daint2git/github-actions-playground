name: random-string
on:
  pull_request:
    branches:
      - "**"
    types: [opened, reopened, synchronize]
jobs:
  random-string:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Random string
        id: xxx      
        uses: ./actions/random-string-action
      - run: |
          echo "TOKEN=123abc" >> $GITHUB_ENV
          echo "::add-mask::$TOKEN"
        shell: bash
      - run: echo This is my token $TOKEN